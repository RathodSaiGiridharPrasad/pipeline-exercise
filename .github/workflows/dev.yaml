# .github/workflows/deploy-india.yml
name: Deploy l4 Backend

on:
  push:
    branches:
      - dev

jobs:
  call-common:
    uses: ./.github/workflows/common.yaml
    with:
      service-name: l4-backend
      environment: dev
